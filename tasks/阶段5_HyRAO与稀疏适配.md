# 阶段 5：Superseded Legacy Entry

本文件只保留给旧中文命令兼容。HyRAO / sparse adaptation / FISA-HISA 路线不再是
active paper-main 方法或当前 protocol surface。

当前 active Phase 5 入口：

```text
tasks/phase5_hyperda_safe_zero_few_shot.md
```

当前主协议：

```text
HyperDA-SAFE: Source-Anchored Few-Shot Operator Refinement
V4.4 zero/few-shot target generalization
K in {0,4,12}
target_context=2015-2021 input-side only
target_support=K labeled cycles
target_val=unused_in_main_protocol
target_eval=2023-2025 final offline evaluation only
```
